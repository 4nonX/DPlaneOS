# Inter Variable font
The Inter-Variable.ttf font can be downloaded from:
https://github.com/rsms/inter/releases/latest

Or use the included prepare-offline.sh script:
  ./scripts/prepare-offline.sh

Note: The system uses system fonts by default, so Inter is optional.
