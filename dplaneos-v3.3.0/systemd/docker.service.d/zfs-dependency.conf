# D-PlaneOS: Prevent Docker from starting before ZFS pools are mounted.
# Without this, containers with ZFS-backed volumes write to empty mountpoint
# directories on the root filesystem, causing silent data loss when ZFS mounts.
[Unit]
After=dplaneos-zfs-mount-wait.service
Requires=dplaneos-zfs-mount-wait.service
